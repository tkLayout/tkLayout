// Example config file:
// This config file is case sensitive
// as a general rule all the objects are capitalized (Tracker, Endcap)
// all the parameter names start with smallcaps and capital letters mark the start
// of a new word (stripCost, innerRadius, ...)

// You can declare only one Tracker and as many Barrel and Endcap as you wish
// You must declare the TRACKER before all its components

// You can daclare only one Tracker
Tracker 2Ptlayer_Barrelonly_noDS {
    zError = 70;
    smallDelta = 2;
    bigDelta = 12;
    overlap = 1;
    etaCut = 2.55;
    ptCost = 200;      // CHF / cm^2
    stripCost = 40;    // CHF / cm^2
    ptPower = 0.1;     // mW / channel
    stripPower = 0.6;  // mW / channel
}


Barrel TOB {
    // Mandatory parameters
    nLayers = 4;
    nModules = 27;
    innerRadius = 524;
    outerRadius = 1080 ;

    // Directives and options
    directive = 2/S;   // used to be # directiveLayer
    directive = 3/S;
}

Barrel PTBARREL {
    // Mandatory parameters
    nLayers = 2;
    nModules = 21;
    innerRadius = 230;
    outerRadius = 350 ;
   // Directives and options
    directive = 2/S;   // used to be # directiveLayer
 }


 Barrel SHORT1 {
    // Mandatory parameters
    nLayers = 2;
    nModules = 12;
    innerRadius = 435;
    outerRadius = 610 ;
    minimumZ = 1380;	
    // Directives and options
}


 Barrel SHORT2 {
    // Mandatory parameters
    nLayers = 2;
    nModules = 4;
    innerRadius = 785;
    outerRadius = 970 ;
    minimumZ = 2110;	
//    minimumZ = 1930;	
    // Directives and options
}


